# in the backend we are going to develop the application full backend and super admin .however we will first stat with backend to integrate theflutter application frontend 

backend E:\4directions\Products\mobile_app\4dfarmer-backend

- backend must use MYSQL database design
- All modules will use real datase now  will no longer be used but we will use the real backend in dev 
- we must first start with login/registration flo correctly in these development
- the regirstration flow will be that farmers must choose a subscriptions plan  3 of them 
- The registration flow must 

In 4dfarmer-backend develop first only the backend in full and not ui . we have flutter for u integration now
 we must plan the backend development structure in the folder following next js best practices of development and quality design.
 Login /register must fully alow the login process and no functionality will working without login module

