# Database
HOST=148.251.246.72
BD_NAME=punfrohs_d4-farming
BD_USER=punfrohs_d4-farming
BD_PASS=Ze3g5cJC3xbA4T8AuGC4
# Auth
JWT_SECRET=                        # min 32 chars, HS256
ACCESS_TOKEN_TTL=900               # seconds (15 min)
REFRESH_TOKEN_TTL=2592000          # seconds (30 days)

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=                         # Gmail app password or SMTP credentials
EMAIL_FROM=noreply@4dfarmer.app

# App
APP_URL=https://api.4dfarmer.app
PORT=3000
NODE_ENV=production

# 3rd-party integrations
WEATHER_API_KEY=
ADVISOR_API_KEY=
DISEASE_API_KEY=

# Redis (optional — falls back to in-memory if unset; required for multi-instance deploys)
REDIS_URL=redis://localhost:6379

# Social Login (optional — social login disabled if unset)
GOOGLE_CLIENT_ID=                  # Google OAuth 2.0 Client ID (from Google Cloud Console)
APPLE_CLIENT_ID=                   # Apple Services ID (e.g. com.4dfarmer.app)
FACEBOOK_APP_ID=                   # Facebook App ID (from Meta Developer Console)
FACEBOOK_APP_SECRET=               # Facebook App Secret
